# Local run configuration (loaded by docker-compose).
#
# With no CLI args the writer takes its input from the single *.nwb file in
# INPUT_DIR and publishes the bundle into OUTPUT_DIR; these mirror the volume
# mounts in docker-compose.yml.
INPUT_DIR=/data/input
OUTPUT_DIR=/data/output
